from setuptools import setup, find_packages

setup(
    name="shared",
    version="0.1.0",
    description="Shared utilities for Surveillance System",
    packages=find_packages(),
    include_package_data=True,
)
