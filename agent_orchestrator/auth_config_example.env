# Authentication Configuration Example for Agent Orchestrator
# 
# This file shows how to configure the Agent Orchestrator Service
# with API key authentication enabled.

# Environment Variables for Authentication
API_KEY=your_master_api_key_here_32_chars_min
ENABLE_RATE_LIMITING=true
DEBUG_MODE=false

# Database Configuration (existing)
DATABASE_URL=postgresql://user:password@localhost/agent_orchestrator

# Service Configuration (existing)
SERVICE_NAME=agent_orchestrator
SERVICE_VERSION=1.0.0
LOG_LEVEL=INFO

# Authentication-specific Settings
# 
# API_KEY: Optional master API key with full permissions
#          If set, creates a master key with admin privileges
#          Should be at least 32 characters for security
#
# ENABLE_RATE_LIMITING: Enable per-API-key rate limiting
#                       Default: true
#
# Example deployment with Docker:
# 
# docker run -d \
#   -e API_KEY="your_secure_master_key_here_32_chars" \
#   -e ENABLE_RATE_LIMITING=true \
#   -e DATABASE_URL="postgresql://user:pass@db/agent_orchestrator" \
#   -p 8000:8000 \
#   agent-orchestrator:latest
